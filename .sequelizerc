const path = require('path');

module.exports = {
  config: path.resolve(__dirname, 'server', 'config', 'env.js'),
  'migrations-path': path.resolve(
    __dirname,
    'server',
    'database',
    'migrations'
  ),
};
